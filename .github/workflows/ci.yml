name: CI
on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  check-versions:
    runs-on: ubuntu-latest
    outputs:
      build_matrix: ${{ steps.get-matrix.outputs.build_matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3

      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0
        with:
          python-version: "3.13"
          enable-cache: true

      - name: Install dependencies
        run: uv sync --frozen

      - name: Get build matrix
        id: get-matrix
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: echo "build_matrix=$(uv run main.py get-matrix)" >> "$GITHUB_OUTPUT"

      - name: Clear older runs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh run list -L400 --repo "$GITHUB_REPOSITORY" --json databaseId -q '.[].databaseId' | tail -n+91 | xargs -IID gh api "repos/$GITHUB_REPOSITORY/actions/runs/ID" -X DELETE || true

  build-morphe:
    name: build (Morphe)
    needs: check-versions
    if: contains(fromJson(needs.check-versions.outputs.build_matrix), 'morphe')
    uses: ./.github/workflows/build.yml
    with:
      patch_source: 'morphe'
    secrets: inherit

  build-piko:
    name: build (Piko)
    needs: check-versions
    if: contains(fromJson(needs.check-versions.outputs.build_matrix), 'piko')
    uses: ./.github/workflows/build.yml
    with:
      patch_source: 'piko'
    secrets: inherit